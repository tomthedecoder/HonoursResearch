from NetworkStructure import *


@dataclass(unsafe_hash=True)
class KuramotoStructure(NetworkStructure):
    """ An aliases to the Network Structure class, since no additions are needed to the parent class"""

    pass